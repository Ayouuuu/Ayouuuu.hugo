pull_request_rules:
  - name: automatic merge for Dependabot pull requests
    conditions:
      - author=dependabot[bot]
      - label=dependencies
      - check-success=deploy/netlify
    actions:
      merge:
        method: merge